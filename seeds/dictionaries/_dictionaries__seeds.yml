seeds:
  - name: city_region
    description: Сопоставление городов и регионов
    config:
      delimetr: ","
      column_types:
        city: varchar(50)
        region: varchar(50)
    columns:
      - name: city
        tests:
        - not_null
        - unique
